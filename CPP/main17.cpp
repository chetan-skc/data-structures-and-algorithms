#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n,l,m,k;
        cin >> n >> l >> m;

        for(int i=0;i<m;i++){
            vector<int> v;
            cin >> k;
            int a[k];
            for(int i=0;i<k;i++){
                cin >> a[i];
            }
            v.push_back();
        }
    int b[l];
    for(int i=0;i<l;i++){
        cin >> b[i];
    }
    for(int i=0;i<l;i++){
        
    }
    }

    return 0;
}