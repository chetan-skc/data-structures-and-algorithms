#include <bits/stdc++.h>
#include <algorithm>
using namespace std;
typedef long long int lli;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    lli t;
    cin >> t;
    while (t--)
    {
        int p;
        cin >> p;

        // if(p%2 != 0){
        //     p-=1;
        // }

        cout << 2 << " " << p-1 << endl;

    }
    return 0;
}
